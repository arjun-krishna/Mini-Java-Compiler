1) Have you tarred + gzipped the file as XXXXXXXX.tar (your 8-digit roll number) ?
-- yes

2) Does your tar file expand (by using the command tar xvzf) to the base directory named XXXXXXXX (your 8-digit roll number) ?
-- yes

3) Have you removed all the class files and jar files from your tar ball?
-- yes

4) Does your base folder contain P2.java that can be compiled using javac?
-- yes

5) Does your P2.java take input from the standard input (for usage of redirection) ?
-- yes

6) Does your P2.java write output to the standard output (for usage of redirection) ?
-- yes

7) Have you removed all the debugging messages that your program may output to the standard output?
-- yes

8) Is (some part of) the assignment plagiarized?
-- no
8.1) If yes, Plagiarism Declarations (if any):

9) Is this checklist present in your folder XXXXXXXX (your 8-digit roll number) ?
-- yes

10) Is this checklist named "ChecklistP2.txt" ?
-- yes

Any comments on your implementation strategy:

1) Mainted a symbol table having a hashmap for each class
2) Did one pass to obtain the symbol table and another pass for type checking
